datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://testes:Givjv9Dv7RTGtuA@cluster0.ewnni.mongodb.net/teste"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       String @id @default(cuid()) @map("_id")
  nome     String
  email    String @unique
  password String

  Question Question[]
}

model Question {
  id          String @id @default(cuid()) @map("_id")
  titulo      String
  descricao   String
  author      User   @relation(fields: [authorId], references: [id])
  areaAtuacao String
  authorId    String
}
